#pragma once

#include "ghostty.h"

bool ghostty_actions_action_cb(ghostty_app_t    app,
                               ghostty_target_s target,
                               ghostty_action_s action);
