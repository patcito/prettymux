diff --git a/include/ghostty.h b/include/ghostty.h
index 646f32fe8..4f316576c 100644
--- a/include/ghostty.h
+++ b/include/ghostty.h
@@ -1089,6 +1089,7 @@ bool ghostty_surface_process_exited(ghostty_surface_t);
 void ghostty_surface_refresh(ghostty_surface_t);
 void ghostty_surface_draw(ghostty_surface_t);
 void ghostty_surface_display_realized(ghostty_surface_t);
+void ghostty_surface_display_unrealized(ghostty_surface_t);
 void ghostty_surface_init_opengl(ghostty_surface_t);
 void ghostty_surface_draw_frame(ghostty_surface_t);
 void ghostty_surface_set_content_scale(ghostty_surface_t, double, double);
diff --git a/build.zig b/build.zig
--- a/build.zig
+++ b/build.zig
@@ -141,5 +141,6 @@ pub fn build(b: *std.Build) !void {
         if (!config.target.result.os.tag.isDarwin()) {
             libghostty_shared.installHeader(); // Only need one header
             libghostty_shared.install("libghostty.so");
             libghostty_static.install("libghostty.a");
+            resources.install();
         }
diff --git a/src/apprt/embedded.zig b/src/apprt/embedded.zig
index e8df465d9..107634ca1 100644
--- a/src/apprt/embedded.zig
+++ b/src/apprt/embedded.zig
@@ -1724,9 +1724,13 @@ pub const CAPI = struct {
     export fn ghostty_surface_display_realized(surface: *Surface) void {
         surface.core_surface.renderer.displayRealized() catch |err| {
             log.warn("failed to reinitialize display err={}", .{err});
         };
     }
-
+
+    export fn ghostty_surface_display_unrealized(surface: *Surface) void {
+        surface.core_surface.renderer.displayUnrealized();
+    }
+
     /// Initialize OpenGL function pointers for the surface.
     /// Call this from a GtkGLArea "realize" callback when the GL context
     /// is current for the first time. This loads GLAD so the renderer
