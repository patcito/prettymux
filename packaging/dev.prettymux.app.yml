app-id: dev.prettymux.app
runtime: org.gnome.Platform
runtime-version: '46'
sdk: org.gnome.Sdk
command: prettymux
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --talk-name=org.freedesktop.Flatpak
  - --filesystem=home

modules:
  - name: ghostty
    buildsystem: simple
    build-commands:
      - install -Dm644 zig-out/lib/libghostty.so /app/lib/libghostty.so
      - cp -r include /app/include
      - mkdir -p /app/vendor
      - cp -r vendor/glad /app/vendor/glad
    sources:
      - type: dir
        path: ../ghostty

  - name: prettymux
    buildsystem: meson
    config-opts:
      - -Dghostty_dir=/app
    subdir: src/gtk
    post-install:
      - install -Dm644 prettymux-shell-integration.sh /app/share/prettymux/shell-integration.sh || true
      - install -Dm644 prettymux-bashrc.sh /app/share/prettymux/prettymux-bashrc.sh || true
      - install -Dm755 bin/open /app/share/prettymux/bin/open || true
      - install -Dm755 bin/xdg-open /app/share/prettymux/bin/xdg-open || true
      - install -Dm644 ../../packaging/prettymux.desktop /app/share/applications/dev.prettymux.app.desktop || true
      - install -Dm644 ../../packaging/prettymux.svg /app/share/icons/hicolor/scalable/apps/prettymux.svg || true
      - install -Dm644 ../../packaging/prettymux.svg /app/share/icons/hicolor/scalable/apps/dev.prettymux.app.svg || true
    sources:
      - type: dir
        path: ..
